import { useState } from "react"
/* import validateUrl from "../validation/validationChecks"

const useFormValidator = (formData) => {
  cosnt[(URL, setUrl)] = useState({
    url: {
      focused: false,
      error: false,
      message: "",
    },
  })

  const validateformUrl = ({
    formData,
    field,
    errors,
    applyInputFocused = false,
  }) => {
    let isValid = true
    const {url} = formData
    let updatedErrors = JSON.parse(JSON.stringify(errors))
  }
  return {}
} */
